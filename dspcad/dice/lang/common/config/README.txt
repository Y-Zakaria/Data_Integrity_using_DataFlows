 
Configuration files for dicelang that are common across different
languages.

The configuration for the "common" sub-package is different from the
top-level configuration in dicelang --- the latter refers to the
overall dicelang package configuration, whereas the former refers
specifically to a sub-package of features that used across languages.
